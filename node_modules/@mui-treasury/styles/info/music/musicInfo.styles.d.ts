import { Theme } from '@material-ui/core';
export declare type MusicInfoClassKey = keyof ReturnType<typeof musicInfoStyles>;
declare const musicInfoStyles: ({ palette }: Theme) => {
    title: {
        fontFamily: string;
        fontSize: string;
        lineHeight: number;
    };
    subtitle: {
        fontFamily: string;
        color: string;
        fontSize: string;
        lineHeight: number;
    };
    caption: {
        fontFamily: string;
        color: string;
        fontSize: string;
        lineHeight: number;
    };
};
export default musicInfoStyles;
