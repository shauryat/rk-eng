/// <reference types="react" />
import { BoxProps } from '@material-ui/core/Box';
import { Position } from './core';
export declare type ItemProps = {
    position?: Position;
    'data-flexindex'?: number;
    grow?: boolean | number;
    shrink?: number;
    cssPosition?: 'fixed' | 'absolute' | 'sticky' | 'static' | 'relative';
} & BoxProps;
declare const Item: ({ className, position, cssPosition, grow, shrink, "data-flexindex": itemIndex, ...props }: ItemProps) => JSX.Element;
export default Item;
